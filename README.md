# Anurag Birthday Website ♡

A private birthday website made for Anurag.

## GitHub Pages
Upload the files in this repository to a GitHub repository, then enable:

**Settings → Pages → Deploy from a branch → main → / (root)**

The site is a single self-contained `index.html`, so all photos are embedded and no image folder is required.
